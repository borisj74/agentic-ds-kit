{
  "id": "scoreboard",
  "status": "stable",
  "intent": "Metrics strip of kit Scorecards. Two to four fill the row; four stay in one row until the strip narrows. More than four compose Conveyor.",
  "useWhen": [
    "Showing two or more KPIs side by side at the top of a dashboard",
    "Replacing ad-hoc flex rows of metric boxes"
  ],
  "doNotUseWhen": [
    "A single KPI — render Scorecard directly",
    "Vertical stacks of metrics — not supported by this contract"
  ],
  "contains": [
    "scorecard",
    "conveyor"
  ],
  "props": {
    "items": {
      "type": "array",
      "itemContract": "scorecard",
      "required": true,
      "minItems": 2,
      "note": "2–4 fill equal columns. Four stay four-up until the strip narrows. More than 4 compose kit Conveyor."
    },
    "scroll": {
      "type": "boolean",
      "default": true,
      "note": "false renders the overflow strip without Conveyor so a page can wrap Scoreboard in its own Conveyor."
    }
  },
  "tokens": [
    "surface.page",
    "layout.metricsMin",
    "space.4"
  ],
  "a11y": {
    "element": "section",
    "requirements": [
      "Expose aria-label describing the metrics group",
      "Each child Scorecard retains its own semantics",
      "More than four items pass aria-label to kit Conveyor as label"
    ]
  },
  "doNot": [
    "Do not invent a local Scoreboard",
    "Do not fake dividers with CSS borders between items",
    "Do not restyle Scorecard padding or borders inside Scoreboard",
    "Do not inline Scorecard markup — import from ui/Scorecard",
    "More than four items — Scoreboard composes kit Conveyor, do not invent a local scroller",
    "Do not wrap a scrolling Scoreboard in Conveyor — pass scroll={false}"
  ],
  "snippet": "<Scoreboard items={[{ label: 'Revenue', value: '$48.2k', delta: '+12.4%', trend: 'up' }, { label: 'Churn', value: '1.2%', delta: '-0.3%', trend: 'down' }, { label: 'Users', value: '12.4k', delta: '+4.1%', trend: 'up' }, { label: 'NPS', value: '64', delta: '+2', trend: 'up' }]} />"
}
