{
  "id": "chat",
  "status": "stable",
  "intent": "Assistant workspace with messages, suggestions, composer, and thinking state.",
  "contains": ["button", "thinkinganimation", "input chrome"],
  "doNot": [
    "Do not add a ChatMessage primitive unless the contract index lists one",
    "Do not restyle kit controls inside the pattern shell",
    "Do not use Modal for inline assistant panels"
  ],
  "snippet": "<ChatPattern title=\"Workspace assistant\" messages={messages} onSend={handleSend} isThinking={busy} />"
}
