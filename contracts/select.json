{
  "id": "select",
  "status": "stable",
  "intent": "Choose one option from a short list. Field trigger, kit DropdownMenu panel. Not a native select.",
  "useWhen": [
    "Choosing one value in a Field",
    "Short fixed lists such as month, year, or timezone"
  ],
  "doNotUseWhen": [
    "Long searchable lists — use Command",
    "Actions from a button — use DropdownMenu",
    "Mutually exclusive choices with visible options — prefer RadioGroup",
    "Multi-select — not supported"
  ],
  "props": {
    "id": {
      "type": "string",
      "required": true
    },
    "size": {
      "type": "enum",
      "values": [
        "md",
        "sm"
      ],
      "default": "md"
    },
    "options": {
      "type": "array",
      "itemShape": {
        "value": "string",
        "label": "string"
      },
      "required": true
    },
    "disabled": {
      "type": "boolean",
      "default": false
    },
    "error": {
      "type": "boolean",
      "default": false
    },
    "defaultValue": {
      "type": "string",
      "required": false
    },
    "name": {
      "type": "string",
      "required": false
    },
    "ariaLabel": {
      "type": "string",
      "required": false,
      "note": "When there is no Field label, such as a DataTable Cell."
    }
  },
  "tokens": [
    "text.primary",
    "surface.card",
    "surface.muted",
    "border.faint",
    "border.focus",
    "status.danger"
  ],
  "a11y": {
    "element": "button",
    "requirements": [
      "Trigger is a field-styled button with aria-haspopup=menu",
      "Must have associated label via Field or ariaLabel",
      "Each option needs a human-readable label"
    ]
  },
  "doNot": [
    "Do not use a native select",
    "Do not use kit Button as the Select trigger — Select uses DropdownMenu triggerStyle=field",
    "Do not invent a local Select or combobox",
    "Do not use raw hex",
    "Do not use for navigation — use AppNav or links"
  ],
  "snippet": "<Field label=\"Timezone\" htmlFor=\"tz\"><Select id=\"tz\" options={[{ value: 'utc', label: 'UTC' }]} /></Field>"
}
