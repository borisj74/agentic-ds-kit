# Getting started

The **kit** is the npm package `agentic-ds-kit` (`packages/kit`). The Next app in this repo is a playground that consumes it.

Agents consume:

- `packages/kit/contracts/index.json` + the matching contract JSON
- `AGENTS.md` (always-on kit rules)
- The prototype-from-kit skill

Humans consume this page, the playground UI, and (when extending the catalog) the short “add a piece” section below.

## Cursor vs Claude Code

Same kit law, different filenames. Both are git-tracked. Clone the repo; do not paste rules into chat.

| Job | Cursor | Claude Code |
| --- | --- | --- |
| Always-on rules | `.cursor/rules/kit.mdc` | `CLAUDE.md` (imports `AGENTS.md`) |
| Compose a screen | `.cursor/skills/prototype-from-kit/` | `.claude/skills/prototype-from-kit/` |
| Catalog | `packages/kit/contracts/` + `packages/kit/ui/` | same files |

Claude Code reads `CLAUDE.md`, not `AGENTS.md`, unless `CLAUDE.md` imports it. This repo does: the first line of `CLAUDE.md` is `@AGENTS.md`. The Next.js block under it is generated by `next dev` — leave it.

Cursor already has the rule. Claude users need **Claude Code** in the cloned repo (CLI or the Claude Code IDE). claude.ai chat will not load these files unless the person is in a Claude Code / Cowork session on the project.

Confirm Claude loaded them: run `/context` and look for `AGENTS.md` / `CLAUDE.md` under memory files. The skill should appear as `/prototype-from-kit` or fire when they ask to build a playground screen.

## Who this is for

| Person | What to do |
| --- | --- |
| Anyone browsing | Clone, `npm run dev`, click around `/components` and `/patterns` |
| Engineer prototyping in Cursor / Claude Code | Same, plus keep the always-on rule and skill in the repo |
| Engineer adding a Button-level piece | Read **Add a component** |
| Engineer adding a screen type | Read **Add a pattern** |

This repo is an npm **workspace**. The published package is `agentic-ds-kit`. The playground is not published.

## Prerequisites

- Node.js 20+
- npm
- Git
- Optional: [Cursor](https://cursor.com) or Claude Code (so the rule and skill load automatically)

## Install and run the playground

```bash
git clone git@github.com:borisj74/agentic-ds-kit.git
cd agentic-ds-kit
npm install
npm run dev
```

Open [http://localhost:43123](http://localhost:43123).

| Route | What you get |
| --- | --- |
| `/` | Introduction |
| `/foundations` | Token scales |
| `/components` | Gallery and per-component docs |
| `/patterns` | Screen blueprints (Dashboard, Activity, Inbox, …) |

```bash
npm run build    # tokens + production build
npm run lint
```

Tokens in `packages/kit/lib/tokens.css` are generated. After editing `packages/kit/tokens/tokens.json`:

```bash
npm run generate:tokens
```

## Point an agent at the kit

Do this in **this repo**. Do not paste the rules into chat every session.

1. Keep `AGENTS.md` (source of the six kit rules).
2. Keep `CLAUDE.md` (Claude Code; starts with `@AGENTS.md`).
3. Keep `.cursor/rules/kit.mdc` (Cursor always-on).
4. Keep both skill copies: `.cursor/skills/prototype-from-kit/` and `.claude/skills/prototype-from-kit/`.

The rule is short on purpose:

1. Read `contracts/index.json`, then only the matching contract file(s).
2. If a contract exists, use the kit component from `ui/`. Never invent a local cousin.
3. If a pattern in `contracts/patterns/` matches the screen, compose from `ui/patterns/`.
4. If something is missing from the index, stop and ask — do not add a one-off.
5. Semantic tokens from `lib/tokens.css` only. No hex in components. No primitive `--color-*` ramps in component CSS.
6. One `variant="primary"` Button per view.

**Eval prompts** (also in the README) are the smoke test that a coworker’s agent is actually using the kit.

### Use the kit from another app

```bash
npm install agentic-ds-kit
```

Next.js must compile the package (TypeScript + CSS modules):

```ts
const nextConfig = {
  transpilePackages: ["agentic-ds-kit"],
};
```

```tsx
import "agentic-ds-kit/tokens.css";
import { Button, Scoreboard, ActivityPattern } from "agentic-ds-kit";
```

Copy into the app root (from `node_modules/agentic-ds-kit/` or `packages/kit/`):

```
AGENTS.md
CLAUDE.md
skills/prototype-from-kit/   →  .cursor/skills/ and .claude/skills/
```

Do not fork components inside the product. If the index has no match, ask — do not add a local cousin.

## How the catalog works

Three groups in `contracts/index.json`:

| `group` | Meaning | Example |
| --- | --- | --- |
| `foundation` | Tokens | `packages/kit/tokens/tokens.json` |
| `component` | One UI piece and its props | `packages/kit/contracts/carousel.json` → `packages/kit/ui/Carousel/` |
| `pattern` | A screen that only composes components | `packages/kit/contracts/patterns/activity.json` → `packages/kit/ui/patterns/ActivityPattern/` |

A **component** contract has `props`, `doNot`, tokens, a11y. A **pattern** contract has `contains` (which components) and `doNot` (what not to invent). Patterns are contracts. They are not a field inside Button.

If the index has no match, the agent must ask. That is the product, not a gap in the docs.

## Add a component

Only when nothing in the index covers the UI.

1. Add an entry to `packages/kit/contracts/index.json` (`group`: `component`).
2. Add `packages/kit/contracts/{id}.json` with intent, props, tokens, a11y, `doNot`, snippet.
3. Implement `packages/kit/ui/{Name}/` using semantic tokens only. Named exports. No local cousins.
4. Re-export from `packages/kit/src/index.ts` if the barrel does not pick it up.
5. Add playground docs under `app/components/` and a nav link if the gallery expects one.
6. Run the playground and click the new piece end to end.

Stop if you were about to add a one-off in `app/` instead of a contract.

## Add a pattern

Only when the screen is a repeated composition of existing components.

1. Add an entry to `packages/kit/contracts/index.json` (`group`: `pattern`).
2. Add `packages/kit/contracts/patterns/{id}.json` with `contains` and `doNot`.
3. Implement `packages/kit/ui/patterns/{Name}Pattern/` from kit components only.
4. Re-export from `packages/kit/src/index.ts`.
5. Wire playground nav (`lib/playground-nav.ts`) and Agentix (`app/_components/DashboardDemo.tsx`) if it is a product screen.
6. Mention it in `packages/kit/skills/prototype-from-kit/SKILL.md` (and the Cursor/Claude copies).

## GitHub and remotes

Public clone: [https://github.com/borisj74/agentic-ds-kit](https://github.com/borisj74/agentic-ds-kit)

```bash
git clone git@github.com:borisj74/agentic-ds-kit.git
cd agentic-ds-kit
npm install
npm run dev
```

Coworkers do not need Cursor Origin. Cursor users and Claude Code users both get the agent files from git.

This workspace keeps **origin** on Cursor Origin as a private mirror. GitHub is the **github** remote.

Optional: deploy the playground (Vercel) so non-git people can browse.

Publish the kit (not the playground):

```bash
npm publish -w agentic-ds-kit --access public
```

You must be logged in to npm (`npm login`). Confirm the name `agentic-ds-kit` is free: `npm view agentic-ds-kit`.
